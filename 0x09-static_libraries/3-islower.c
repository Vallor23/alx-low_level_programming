#include "main.h"
/**
 * _islower - program checks for lower case letters
 * @c: the character to check
 *
 * Return: 1 if c is lower case, and 0 if its not
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
